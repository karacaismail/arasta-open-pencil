const page=figma.root.children.find(p=>p.name==='00c · Durum matrisi');const G=figma.graph,V=Object.fromEntries(figma.getLocalVariables().map(v=>[v.name,v.id]));let count=0;
for(const c of page.findAll(n=>n.type==='COMPONENT'&&n.name.includes('State=Focus'))){for(const n of c.children.filter(n=>n.name==='2px focus gap'))n.remove();c.clipsContent=false;const ring=figma.createFrame();c.appendChild(ring);ring.name='Focus ring · 2px gap + 2px stroke';ring.fills=[];ring.strokes=[{type:'SOLID',color:G.resolveColorVariable(V['focus.ring']),opacity:1,visible:true}];ring.strokeWeight=2;ring.cornerRadius=12;ring.resize(c.width+8,c.height+8);ring.layoutPositioning='ABSOLUTE';ring.x=-4;ring.y=-4;figma.bindVariable(ring.id,'strokes/0/color',V['focus.ring']);count++;}
return {focusVariants:count};